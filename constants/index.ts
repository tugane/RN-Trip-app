import { Dimensions } from "react-native";

export const CONTAINER_HEIGHT = 450;
export const LEFT_CONTAINER_WIDTH = 70;
export const TEXT_ITEM_WIDTH = 30;
export const TEXT_ITEM_LENGTH = 100;
export const TRIP_ITEM_WIDTH = 250;
export const { width, height } = Dimensions.get("window");
export const RIGHT_CONTAINER_WIDTH = width - LEFT_CONTAINER_WIDTH;
